# this is an example of the OpenBMP REST API
# as a demonstration of an API spec in YAML
swagger: '2.0'
info: 
  title: OpenBMP REST API
  description: Powering Dashboards with the OpenBMP REST API
  version: "1.0.0"
# the domain of the service
host: api.openbmp.org
# array of all schemes that your API supports
schemes:
  - https
# will be prefixed to all paths
basePath: /v1
produces:
  - application/json
paths:
  /asn_stats:
    get:
      summary: Per ASN Statistics
      description: |
        This As Statistics Endpoint returns a list of all active ASN's in the DB.
      parameters:
        - $ref: '#/parameters/limit'
      tags:
        - As Statistics
      responses:
        200:
          description: An array of all active ASNs in the DB.
          schema:
            type: array
            items:
              $ref: '#/definitions/AsStatistics'
        400:
          description: Client Error
          schema:
            type: array
            items:
              $ref: '#/definitions/Error'
          examples: 
            application/json: |
              $ref: '#/definitions/ErrorDetails'
        500:
          description: OpenBMP Server Error
          schema:
            type: array
            items:
              $ref: '#/definitions/Error'
          examples: 
            application/json: |
              $ref: '#/definitions/ErrorDetails'
        default:
          description: Unexpected error
          schema:
            type: array
            items:
              $ref: '#/definitions/Error'
          examples: 
            application/json: |
              $ref: '#/definitions/ErrorDetails'
  /asn_stats/{asn}:
    get:
      summary: AS Statistics for an ASN
      description: |
        This As Statistics Endpoint returns the AS statistics for the provided ASN.
      parameters:
        - $ref: '#/parameters/asn'
        - $ref: '#/parameters/limit'
      tags:
        - As Statistics
      responses:
        200:
          description: An array of AS Statistics for the provided ASN
          schema:
            type: array
            items:
              $ref: '#/definitions/AsStatistics'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /asn_stats/ipv4:
    get:
      summary: Get the top ASN's originating prefixes or that are transit to other ASN's
      description: This As Statistics Endpoint lists the top ASN's originating prefixes or that are transit to other ASN's.
      parameters:
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/topTransit'
        - $ref: '#/parameters/topOrigin'
      tags:
        - As Statistics
      responses:
        200:
          description: An array of top ASN's originating prefixes or that are transit to other ASN's
          schema:
            type: array
            items:
              $ref: '#/definitions/AsStatisticsV4'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /asn_stats/ipv6:
    get:
      summary: Get the top ASN's originating prefixes or that are transit to other ASN's
      description: This As Statistics Endpoint lists the top ASN's originating prefixes or that are transit to other ASN's
      parameters:
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/topTransit'
        - $ref: '#/parameters/topOrigin'
      tags:
        - As Statistics
      responses:
        200:
          description: An array of top ASN's originating prefixes or that are transit to other ASN's
          schema:
            type: array
            items:
              $ref: '#/definitions/AsStatisticsV6'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /community/part2/{p1}:
    get:
      summary: Community Placeholder
      description: |
        This Community Endpoint returns Community Placeholder.
      parameters:
        - $ref: '#/parameters/p1'
        - $ref: '#/parameters/limit'
      tags:
        - Community
      responses:
        200:
          description: Community Placeholder
          schema:
            type: array
            items:
              $ref: '#/definitions/Community'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /community/part1/{p1}:
    get:
      summary: Community Placeholder
      description: |
        This Community Endpoint returns Community Placeholder.
      parameters:
        - $ref: '#/parameters/p1'
        - $ref: '#/parameters/limit'
      tags:
        - Community
      responses:
        200:
          description: Community Placeholder
          schema:
            type: array
            items:
              $ref: '#/definitions/Community'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /dns/{hostname}:
    get:
      summary: DNS Placeholder
      description: |
        This Dns Endpoint returns placeholder.
      parameters:
        - $ref: '#/parameters/hostname'
      tags:
        - DNS
      responses:
        200:
          description: DNS Placeholder
          schema:
            type: array
            items:
              $ref: '#/definitions/DNS'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /downstream/{asn}:
    get:
      summary: List all downstream ASN's for the given ASN
      description: |
        This downstream Endpoint returns all the downstream ASN's for the given ASN.
      parameters:
        - $ref: '#/parameters/asn'
      tags:
        - Downstream
      responses:
        200:
          description: An array of downstream ASN's for the given ASN
          schema:
            type: array
            items:
              $ref: '#/definitions/AsInfo'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /downstream/{asn}/count:
    get:
      summary: List all downstream ASN's for the given ASN and count the number of distinct prefixes (each prefix/len + peer is counted)
      description: |
        This downstream Endpoint returns all the downstream ASN's for the given ASN and count the number of distinct prefixes.
      parameters:
        - $ref: '#/parameters/asn'
      tags:
        - Downstream
      responses:
        200:
          description: An array of downstream ASN's for the given ASN and count the number of distinct prefixes
          schema:
            type: array
            items:
              $ref: '#/definitions/AsInfoAndCount'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /downstream/{asn}/peer/count:
    get:
      summary: List all downstream ASN's for the given ASN and count the number of distinct prefixes per peer
      description: |
        This downstream Endpoint returns all downstream ASN's for the given ASN and count the number of distinct prefixes per peer.
      parameters:
        - $ref: '#/parameters/asn'
      tags:
        - Downstream
      responses:
        200:
          description: An array of downstream ASN's for the given ASN and count the number of distinct prefixes per peer
          schema:
            type: array
            items:
              $ref: '#/definitions/AsInfoAndCountAndPeer'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /downstream/peer/{peerHashId}:
    get:
      summary: Distinctly lists the ASN's that are directly to the right of the peer ASN. E.g. ASPath's { "10 20, 30", "10 9 8"} would produce a list containing 20 and 9 since those ASNs are to the right of the peering ASN of 10. Each AS will have the ASN name, organization, and country.
      description: |
        This downstream Endpoint returns all the ASN's that are directly to the right of the peer AS.
      parameters:
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/limit'
      tags:
        - Downstream
      responses:
        200:
          description: An array of downstream ASN's that are directly to the right of the peer AS
          schema:
            type: array
            items:
              $ref: '#/definitions/AsInfoAndPeerAsn'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /upstream/{asn}:
    get:
      summary: List all upstream ASN's for the given ASN
      description: |
        This upstream Endpoint returns all the upstream ASN's for the given ASN.
      parameters:
        - $ref: '#/parameters/asn'
      tags:
        - Upstream
      responses:
        200:
          description: An array of upstream ASN's for the given ASN
          schema:
            type: array
            items:
              $ref: '#/definitions/AsInfo'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /upstream/{asn}/count:
    get:
      summary: List all upstream ASN's for the given ASN and count the number of distinct prefixes (each prefix/len + peer is counted)
      description: |
        This upstream Endpoint returns all the upstream ASN's for the given ASN and count the number of distinct prefixes.
      parameters:
        - $ref: '#/parameters/asn'
      tags:
        - Upstream
      responses:
        200:
          description: An array of upstream ASN's for the given ASN and count the number of distinct prefixes
          schema:
            type: array
            items:
              $ref: '#/definitions/AsInfoAndCount'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /upstream/{asn}/peer/count:
    get:
      summary: List all upstream ASN's for the given ASN and count the number of distinct prefixes per peer
      description: |
        This upstream Endpoint returns all upstream ASN's for the given ASN and count the number of distinct prefixes per peer.
      parameters:
        - $ref: '#/parameters/asn'
      tags:
        - Upstream
      responses:
        200:
          description: An array of upstream ASN's for the given ASN and count the number of distinct prefixes per peer
          schema:
            type: array
            items:
              $ref: '#/definitions/AsInfoAndCountAndPeer'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /peer:
    get:
      summary: BGP Peer View
      description: |
        This BGP Peers Endpoint returns a list of BGP peers.
      parameters:
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/withGeo'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - BGP Peers
      responses:
        200:
          description: An array of all BGP peers.
          schema:
            type: array
            items:
              $ref: '#/definitions/VPeers'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /peer/{peerHashId}:
    get:
      summary: Returns a placeholder.
      description: |
        This Bgp Peers Endpoint returns placeholder.
      parameters:
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/withGeo'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - BGP Peers
      responses:
        200:
          description: Placeholder
          schema:
            type: array
            items:
              $ref: '#/definitions/VPeers'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /peer/localip/{ipAddress}:
    get:
      summary: Returns a list of BGP peers by local IP address.
      description: |
        This Bgp Peers Endpoint returns a list of BGP peers by local IP address.
      parameters:
        - $ref: '#/parameters/ipAddress'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/withGeo'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - BGP Peers
      responses:
        200:
          description: An array of BGP peers by local IP address
          schema:
            type: array
            items:
              $ref: '#/definitions/VPeers'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /peer/remoteip/{ipAddress}:
    get:
      summary: Returns a list of BGP peers by remote/peer IP address.
      description: |
        This Bgp Peers Endpoint returns a list of BGP peers by remote/peer IP address.
      parameters:
        - $ref: '#/parameters/ipAddress'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/withGeo'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - BGP Peers
      responses:
        200:
          description: An array of BGP peers by remote IP address
          schema:
            type: array
            items:
              $ref: '#/definitions/VPeers'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /peer/router/{ipAddress}:
    get:
      summary: Returns a list of BGP peers based on Router IP or Router Name.
      description: |
        This Bgp Peers Endpoint returns a list of BGP peers based on Router IP or Router Name.
      parameters:
        - $ref: '#/parameters/ipAddress'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/withGeo'
        - $ref: '#/parameters/orderBy'
      tags:
        - BGP Peers
      responses:
        200:
          description: An array of BGP peers based on Router IP or Router Name
          schema:
            type: array
            items:
              $ref: '#/definitions/VPeers'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /peer/asn/{asn}:
    get:
      summary: Returns a list of BGP peers by Peer ASN.
      description: |
        This Bgp Peers Endpoint returns a list of BGP peers by Peer ASN.
      parameters:
        - $ref: '#/parameters/asn'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/withGeo'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - BGP Peers
      responses:
        200:
          description: An array of BGP peers by remote IP address
          schema:
            type: array
            items:
              $ref: '#/definitions/VPeers'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /peer/type/count:
    get:
      summary: Get the peer counts by status IP version type
      description: This BGP Peers Endpoint lists peer counts by status IP version type.
      tags:
        - BGP Peers
      responses:
        200:
          description: An array of peer counts by status IP version type
          schema:
            type: array
            items:
              $ref: '#/definitions/IpTypeCount'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /peer/type/count/router:
    get:
      summary: Get the list of routers with count of peers by IP Type
      description: This BGP Peers Endpoint lists routers with count of peers by IP Type.
      tags:
        - BGP Peers
      responses:
        200:
          description: An array of routers with count of peers by IP Type
          schema:
            type: array
            items:
              $ref: '#/definitions/RoutersWithPeerCount'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /peer/type/v4:
    get:
      summary: Get the list of IPv4 peers
      description: This BGP Peers Endpoint lists IPv4 peers.
      parameters:
        - $ref: '#/parameters/withGeo'
      tags:
        - BGP Peers
      responses:
        200:
          description: An array of IPv4 peers
          schema:
            type: array
            items:
              $ref: '#/definitions/VPeers'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /peer/router/{ipAddress}/type/v4:
    get:
      summary: Returns a placeholder.
      description: |
        This Bgp Peers Endpoint returns placeholder.
      parameters:
        - $ref: '#/parameters/ipAddress'
        - $ref: '#/parameters/withGeo'
      tags:
        - BGP Peers
      responses:
        200:
          description: Placeholder
          schema:
            type: array
            items:
              $ref: '#/definitions/VPeers'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /peer/type/v6:
    get:
      summary: Get the list of IPv6 peers
      description: This BGP Peers Endpoint lists IPv6 peers.
      parameters:
        - $ref: '#/parameters/withGeo'
      tags:
        - BGP Peers
      responses:
        200:
          description: An array of IPv6 peers
          schema:
            type: array
            items:
              $ref: '#/definitions/VPeers'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /peer/router/{ipAddress}/type/v6:
    get:
      summary: Returns a placeholder.
      description: |
        This Bgp Peers Endpoint returns placeholder.
      parameters:
        - $ref: '#/parameters/ipAddress'
        - $ref: '#/parameters/withGeo'
      tags:
        - BGP Peers
      responses:
        200:
          description: Placeholder
          schema:
            type: array
            items:
              $ref: '#/definitions/VPeers'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /peer/status/count:
    get:
      summary: Get the peer counts by peer status
      description: This BGP Peers Endpoint lists peer counts by peer status.
      tags:
        - BGP Peers
      responses:
        200:
          description: An array of peer counts by peer status
          schema:
            type: array
            items:
              $ref: '#/definitions/StatusTypeCount'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /peer/status/up:
    get:
      summary: Get the list of BGP peers that are UP
      description: This BGP Peers Endpoint lists BGP peers that are UP.
      parameters:
        - $ref: '#/parameters/withGeo'
      tags:
        - BGP Peers
      responses:
        200:
          description: An array of BGP peers that are UP
          schema:
            type: array
            items:
              $ref: '#/definitions/VPeers'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /peer/status/down:
    get:
      summary: Get the list of BGP peers that are DOWN
      description: This BGP Peers Endpoint lists BGP peers that are DOWN.
      parameters:
        - $ref: '#/parameters/withGeo'
      tags:
        - BGP Peers
      responses:
        200:
          description: An array of BGP peers that are DOWN
          schema:
            type: array
            items:
              $ref: '#/definitions/VPeers'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /peer/prefix:
    get:
      summary: Get the list of BGP peers and their Pre-Policy and Post-Policy RIB counts.
      description: This BGP Peers Endpoint lists the BGP peers and their Pre-Policy and Post-Policy RIB counts.
      parameters:
        - $ref: '#/parameters/limit'
      tags:
        - BGP Peers
      responses:
        200:
          description: An array of BGP peers and their Pre-Policy and Post-Policy RIB counts
          schema:
            type: array
            items:
              $ref: '#/definitions/VPeerPrefixReport'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /peer/prefix/{ipAddress}:
    get:
      summary: Returns a list of BGP peers and their Pre-Policy and Post-Policy RIB counts by peer address.
      description: |
        This Bgp Peers Endpoint returns a list of BGP peers and their Pre-Policy and Post-Policy RIB counts by peer address.
      parameters:
        - $ref: '#/parameters/ipAddress'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/withGeo'
        - $ref: '#/parameters/orderBy'
      tags:
        - BGP Peers
      responses:
        200:
          description: An array of BGP peers and their Pre-Policy and Post-Policy RIB counts by peer address
          schema:
            type: array
            items:
              $ref: '#/definitions/VPeerPrefixReport'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /rib:
    get:
      summary: Unicast IPv4/IPv6 RIB/routes view
      description: |
        This Rib Endpoint returns a list of RIB entries.
      parameters:
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - Rib
      responses:
        200:
          description: An array of RIB entries.
          schema:
            type: array
            items:
              $ref: '#/definitions/VRoutes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /rib/peer/{peerHashId}:
    get:
      summary: Returns a placeholder.
      description: |
        This Rib Endpoint returns placeholder.
      parameters:
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - Rib
      responses:
        200:
          description: Placeholder
          schema:
            type: array
            items:
              $ref: '#/definitions/VRoutes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /rib/router/{routerHashId}:
    get:
      summary: Returns a placeholder.
      description: |
        This Rib Endpoint returns placeholder.
      parameters:
        - $ref: '#/parameters/routerHashId'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - Rib
      responses:
        200:
          description: Placeholder
          schema:
            type: array
            items:
              $ref: '#/definitions/VRoutes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /rib/asn/{asn}:
    get:
      summary: Returns a list of prefixes by Origin ASN.
      description: |
        This Rib Endpoint returns list of prefixes by Origin ASN.
      parameters:
        - $ref: '#/parameters/asn'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
        - $ref: '#/parameters/brief'
        - $ref: '#/parameters/distinct'
      tags:
        - Rib
      responses:
        200:
          description: An array of prefixes by Origin ASN
          schema:
            type: array
            items:
              $ref: '#/definitions/VRoutes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /rib/peer/{peerHashId}/asn/{asn}:
    get:
      summary: Returns a placeholder.
      description: |
        This Rib Endpoint returns placeholder.
      parameters:
        - $ref: '#/parameters/asn'
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/brief'
        - $ref: '#/parameters/distinct'
        - $ref: '#/parameters/orderBy'
      tags:
        - Rib
      responses:
        200:
          description: Placeholder
          schema:
            type: array
            items:
              $ref: '#/definitions/VRoutes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /rib/asn/{asn}/count:
    get:
      summary: List all prefixes by Origin ASN
      description: |
        This Rib Endpoint returns all prefixes by Origin ASN.
      parameters:
         - $ref: '#/parameters/asn'
      tags:
        - Rib
      responses:
        200:
          description: An array of all prefixes by Origin ASN
          schema:
            type: array
            items:
              $ref: '#/definitions/GenericCount'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /rib/peer/{peerHashId}/asn/{asn}/count:
    get:
      summary: Placeholder
      description: |
        This Rib Endpoint returns placeholder.
      parameters:
         - $ref: '#/parameters/asn'
         - $ref: '#/parameters/peerHashId'
      tags:
        - Rib
      responses:
        200:
          description: Placeholder
          schema:
            type: array
            items:
              $ref: '#/definitions/GenericCount'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /rib/prefix/{prefix}:
    get:
      summary: Returns a list of prefixes by Origin ASN.
      description: |
        This Rib Endpoint returns list of prefixes by Origin ASN.
      parameters:
         - $ref: '#/parameters/prefix'
         - $ref: '#/parameters/limit'
         - $ref: '#/parameters/where'
         - $ref: '#/parameters/orderBy'
      tags:
        - Rib
      responses:
        200:
          description: An array of prefixes by Origin ASN
          schema:
            type: array
            items:
              $ref: '#/definitions/VRoutes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /rib/peer/{peerHashId}/prefix/{prefix}:
    get:
      summary: Returns a placeholder.
      description: |
        This Rib Endpoint returns placeholder.
      parameters:
         - $ref: '#/parameters/prefix'
         - $ref: '#/parameters/peerHashId'
         - $ref: '#/parameters/limit'
         - $ref: '#/parameters/where'
         - $ref: '#/parameters/orderBy'
      tags:
        - Rib
      responses:
        200:
          description: Placeholder
          schema:
            type: array
            items:
              $ref: '#/definitions/VRoutes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /rib/prefix/{prefix}/{length}:
    get:
      summary: Returns a list of prefixes by Prefix/Length.
      description: |
        This Rib Endpoint returns a list of prefixes by Prefix/Length.
      parameters:
         - $ref: '#/parameters/prefix'
         - $ref: '#/parameters/length'
         - $ref: '#/parameters/limit'
         - $ref: '#/parameters/where'
         - $ref: '#/parameters/orderBy'
      tags:
        - Rib
      responses:
        200:
          description: An array of prefixes by Prefix/Length
          schema:
            type: array
            items:
              $ref: '#/definitions/VRoutes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /rib/peer/{peerHashId}/prefix/{prefix}/{length}:
    get:
      summary: Returns a placeholder.
      description: |
        This Rib Endpoint returns a list of prefixes by Prefix/Length.
      parameters:
         - $ref: '#/parameters/prefix'
         - $ref: '#/parameters/peerHashId'
         - $ref: '#/parameters/length'
         - $ref: '#/parameters/limit'
         - $ref: '#/parameters/where'
         - $ref: '#/parameters/orderBy'
      tags:
        - Rib
      responses:
        200:
          description: Placeholder
          schema:
            type: array
            items:
              $ref: '#/definitions/VRoutes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /rib/lookup/{ipAddress}:
    get:
      summary: Returns the longest match prefix based on the IP (more specific) supplied.
      description: |
        This Rib Endpoint returns the longest match prefix based on the IP (more specific) supplied.
      parameters:
         - $ref: '#/parameters/ipAddress'
         - $ref: '#/parameters/limit'
         - $ref: '#/parameters/where'
         - $ref: '#/parameters/aggregates'
         - $ref: '#/parameters/distinct'
         - $ref: '#/parameters/orderBy'
      tags:
        - Rib
      responses:
        200:
          description: An array of longest match prefix based on the IP (more specific) supplied.
          schema:
            type: array
            items:
              $ref: '#/definitions/PrefixDetails'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /rib/peer/{peerHashId}/lookup/{ipAddress}:
    get:
      summary: Returns the placeholder.
      description: |
        This Rib Endpoint returns placeholder.
      parameters:
         - $ref: '#/parameters/ipAddress'
         - $ref: '#/parameters/peerHashId'
         - $ref: '#/parameters/limit'
         - $ref: '#/parameters/where'
         - $ref: '#/parameters/aggregates'
         - $ref: '#/parameters/distinct'
         - $ref: '#/parameters/orderBy'
      tags:
        - Rib
      responses:
        200:
          description: placeholder.
          schema:
            type: array
            items:
              $ref: '#/definitions/PrefixDetails'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /rib/history/{prefix}/{length}:
    get:
      summary: Returns the RIB history for a given prefix/length.
      description: |
        This Rib Endpoint returns the RIB history for a given prefix/length.
      parameters:
        - $ref: '#/parameters/prefix'
        - $ref: '#/parameters/length'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/hours'
        - $ref: '#/parameters/timestamp'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - Rib
      responses:
        200:
          description: An array of prefixes by Prefix/Length
          schema:
            type: array
            items:
              $ref: '#/definitions/Timestamp'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /rib/withdraws/{prefix}/{length}:
    get:
      summary: Returns the RIB history for a given prefix/length.
      description: |
        This Rib Endpoint returns the RIB history for a given prefix/length.
      parameters:
        - $ref: '#/parameters/prefix'
        - $ref: '#/parameters/length'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/hours'
        - $ref: '#/parameters/timestamp'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - Rib
      responses:
        200:
          description: An array of prefixes by Prefix/Length
          schema:
            type: array
            items:
              $ref: '#/definitions/Timestamp'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /rib/peer/{peerHashId}/history/{prefix}/{length}:
    get:
      summary: Placeholder.
      description: |
        This Rib Endpoint returns Placeholder.
      parameters:
        - $ref: '#/parameters/prefix'
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/length'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/hours'
        - $ref: '#/parameters/timestamp'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - Rib
      responses:
        200:
          description: Placeholder
          schema:
            type: array
            items:
              $ref: '#/definitions/Timestamp'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /rib/peer/{peerHashId}/withdraws/{prefix}/{length}:
    get:
      summary: Placeholder.
      description: |
        This Rib Endpoint returns Placeholder.
      parameters:
        - $ref: '#/parameters/prefix'
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/length'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/hours'
        - $ref: '#/parameters/timestamp'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - Rib
      responses:
        200:
          description: Placeholder
          schema:
            type: array
            items:
              $ref: '#/definitions/Timestamp'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /linkstate/spf/peer/{peerHashId}/ospf/{routerId}:
    get:
      summary: Runs OSPF SPF and generates a RIB for the given bgp-ls peer and router-id. The generated RIB is returned.
      description: |
        This BGP-LS SPF Endpoint runs OSPF SPF and generates a RIB for the given bgp-ls peer and router-id. The generated RIB is returned.
      parameters:
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/routerId'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - BGP-LS SPF
      responses:
        200:
          description: Runs OSPF SPF and generates a RIB for the given bgp-ls peer and router-id. The generated RIB is returned.
          schema:
            type: array
            items:
              $ref: '#/definitions/IgpOspfSpf'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /linkstate/spf/peer/{peerHashId}/isis/{routerId}/{mtId}:
    get:
      summary: Runs IS-IS SPF and generates a RIB for the given bgp-ls peer and router-id. The generated RIB is returned.
      description: |
        This BGP-LS SPF Endpoint runs IS-IS SPF and generates a RIB for the given bgp-ls peer and router-id. The generated RIB is returned.
      parameters:
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/routerId'
        - $ref: '#/parameters/mtId'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - BGP-LS SPF
      responses:
        200:
          description: Runs IS-IS SPF and generates a RIB for the given bgp-ls peer and router-id. The generated RIB is returned.
          schema:
            type: array
            items:
              $ref: '#/definitions/IgpIsisSpf'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /linkstate/nodes:
    get:
      summary: Returns the list of link state nodes.
      description: |
        This BGP-LS Endpoint returns the list of link state nodes
      parameters:
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
        - $ref: '#/parameters/withGeo'
      tags:
        - BGP-LS
      responses:
        200:
          description: Returns the list of link state nodes
          schema:
            type: array
            items:
              $ref: '#/definitions/VLSNodes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /linkstate/nodes/peer/{peerHashId}:
    get:
      summary: Returns the list of link state nodes for the given peer hash id. Get the peer hash ID by listing the nodes.
      description: |
        This BGP-LS Endpoint returns the list of link state nodes for the given peer hash id. Get the peer hash ID by listing the nodes.
      parameters:
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
        - $ref: '#/parameters/withGeo'
      tags:
        - BGP-LS
      responses:
        200:
          description: Returns the list of link state nodes for the given peer hash id. Get the peer hash ID by listing the nodes.
          schema:
            type: array
            items:
              $ref: '#/definitions/VLSNodes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /linkstate/nodes/peer/{peerHashId}/{mtId}:
    get:
      summary: Returns the list of link state nodes for the given peer hash id. Get the peer hash ID by listing the nodes.
      description: |
        This BGP-LS Endpoint returns the list of link state nodes for the given peer hash id. Get the peer hash ID by listing the nodes.
      parameters:
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/mtId'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
        - $ref: '#/parameters/withGeo'
      tags:
        - BGP-LS
      responses:
        200:
          description: Returns the list of link state nodes for the given peer hash id. Get the peer hash ID by listing the nodes.
          schema:
            type: array
            items:
              $ref: '#/definitions/VLSNodes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /linkstate/links:
    get:
      summary: Returns the list of link state links.
      description: |
        This BGP-LS Endpoint returns the list of link state links
      parameters:
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - BGP-LS
      responses:
        200:
          description: Returns the list of link state links
          schema:
            type: array
            items:
              $ref: '#/definitions/VLSLinks'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /linkstate/links/peer/{peerHashId}:
    get:
      summary: Returns the list of link state links for given peer hash id.
      description: |
        This BGP-LS Endpoint returns the list of link state links for given peer hash id.
      parameters:
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - BGP-LS
      responses:
        200:
          description: Returns the list of link state links for given peer hash id.
          schema:
            type: array
            items:
              $ref: '#/definitions/VLSLinks'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /linkstate/links/peer/{peerHashId}/{mtId}:
    get:
      summary: Returns the list of link state links for given peer hash id.
      description: |
        This BGP-LS Endpoint returns the list of link state links for given peer hash id.
      parameters:
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/mtId'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - BGP-LS
      responses:
        200:
          description: Returns the list of link state links for given peer hash id.
          schema:
            type: array
            items:
              $ref: '#/definitions/VLSLinks'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /linkstate/prefixes:
    get:
      summary: Returns the list of link state prefixes.
      description: |
        This BGP-LS Endpoint returns the list of link state prefixes
      parameters:
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - BGP-LS
      responses:
        200:
          description: Returns the list of link state prefixes
          schema:
            type: array
            items:
              $ref: '#/definitions/VLSPrefixes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /linkstate/prefixes/peer/{peerHashId}:
    get:
      summary: Returns the list of link state prefixes for given peer hash id.
      description: |
        This BGP-LS Endpoint returns the list of link state prefixes for given peer hash id.
      parameters:
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - BGP-LS
      responses:
        200:
          description: Returns the list of link state prefixes for given peer hash id.
          schema:
            type: array
            items:
              $ref: '#/definitions/VLSPrefixes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /linkstate/prefixes/peer/{peerHashId}/{mtId}:
    get:
      summary: Returns the list of link state prefixes for given peer hash id.
      description: |
        This BGP-LS Endpoint returns the list of link state prefixes for given peer hash id.
      parameters:
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/mtId'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - BGP-LS
      responses:
        200:
          description: Returns the list of link state prefixes for given peer hash id.
          schema:
            type: array
            items:
              $ref: '#/definitions/VLSPrefixes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /linkstate/peers:
    get:
      summary: Returns placeholder.
      description: |
        This BGP-LS Endpoint returns placeholder.
      parameters:
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
        - $ref: '#/parameters/withGeo'
      tags:
        - BGP-LS
      responses:
        200:
          description: Returns placeholder.
          schema:
            type: array
            items:
              $ref: '#/definitions/VLSPeers'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /geoip/{ipAddress}:
    get:
      summary: Returns the geolocation data based on the IP supplied
      description: |
        This GEOIP Endpoint returns the geolocation data based on the IP supplied.
      parameters:
        - $ref: '#/parameters/ipAddress'
        - $ref: '#/parameters/where'
      tags:
        - GeoIP
      responses:
        200:
          description: An array of geolocation data based on the IP supplied
          schema:
            type: array
            items:
              $ref: '#/definitions/GeoIPEmbed'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /whois/asn:
    get:
      summary: Returns all whois information
      description: This Who Is Endpoint returns all whois information
      parameters:
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/limit'
      tags:
        - Who Is Asn
      responses:
        200:
          description: An array of all whois information
          schema:
            type: array
            items:
              $ref: '#/definitions/GenWhoIsAsn'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /whois/asn/count:
    get:
      summary: Returns the count of ASN matches based on WHERE clause
      description: This Who Is Endpoint returns the count of ASN matches based on WHERE clause
      parameters:
        - $ref: '#/parameters/where'
      tags:
        - Who Is Asn
      responses:
        200:
          description: Returns the count of ASN matches based on WHERE clause
          schema:
            type: array
            items:
              $ref: '#/definitions/GenericCount'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /whois/asn/{asn}:
    get:
      summary: Returns the whois info for the given ASN.
      description: |
        This Who Is Endpoint returns the whois info for the given ASN.
      parameters:
        - $ref: '#/parameters/asn'
      tags:
        - Who Is Asn
      responses:
        200:
          description: Returns the whois info for the given ASN
          schema:
            type: array
            items:
              $ref: '#/definitions/GenWhoIsAsn'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /whois/all:
    get:
      summary: Placeholder
      description: This Who Is Endpoint returns placeholder
      tags:
        - Who Is Asn
      responses:
        200:
          description: Placeholder
          schema:
            type: array
            items:
              $ref: '#/definitions/AsInfoLatLong'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /whois/{asn}/related:
    get:
      summary: Placeholder.
      description: |
        This Who Is Endpoint returns placeholder.
      parameters:
        - $ref: '#/parameters/asn'
      tags:
        - Who Is Asn
      responses:
        200:
          description: placeholder
          schema:
            type: object
            items:
              $ref: '#/definitions/UpDown'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /whois/prefix/{prefix}/{length}:
    get:
      summary: placeholder.
      description: |
        This Who Is Endpoint returns placeholder.
      parameters:
        - $ref: '#/parameters/prefix'
        - $ref: '#/parameters/length'
        - $ref: '#/parameters/where'
      tags:
        - Who Is Prefix
      responses:
        200:
          description: Returns the placeholder
          schema:
            type: array
            items:
              $ref: '#/definitions/GenWhoIsRoute'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /whois/prefix/from/{asn}:
    get:
      summary: placeholder.
      description: |
        This Who Is Endpoint returns the whois info for the given ASN.
      parameters:
        - $ref: '#/parameters/asn'
      tags:
        - Who Is Prefix
      responses:
        200:
          description: Returns the placeholder
          schema:
            type: array
            items:
              $ref: '#/definitions/VRoutes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /routers:
    get:
      summary: BMP Router view
      description: |
        This Routers Endpoint returns a list of BMP routers currently known in the database.
      parameters:
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/withGeo'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - Routers
      responses:
        200:
          description: An array of all BMP routers currently known in the database.
          schema:
            type: array
            items:
              $ref: '#/definitions/Router'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /routers/status/count:
    get:
      summary: Returns a count of DOWN and UP BMP routers
      description: This Routers Endpoint returns a count of DOWN and UP BMP routers
      tags:
        - Routers
      responses:
        200:
          description: Returns a count of DOWN and UP BMP routers
          schema:
            type: array
            items:
              $ref: '#/definitions/RouterStatus'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /routers/status/up:
    get:
      summary: Returns a list of BMP routers that are up
      description: This Routers Endpoint returns the list of BMP routers that are up
      parameters:
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - Routers
      responses:
        200:
          description: Returns a list of BMP routers that are up
          schema:
            type: array
            items:
              $ref: '#/definitions/Router'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /routers/status/down:
    get:
      summary: Returns a list of BMP routers that are down
      description: This Routers Endpoint returns the list of BMP routers that are down
      parameters:
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/where'
        - $ref: '#/parameters/orderBy'
      tags:
        - Routers
      responses:
        200:
          description: Returns a list of BMP routers that are down
          schema:
            type: array
            items:
              $ref: '#/definitions/Router'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /updates/top:
    get:
      summary: Get the list of top prefixes by number of updates in past 'hours'. Limit defaults to 25 and hours defaults to 2.
      description: This Updates Endpoint returns the top prefixes by number of updates in past 'hours'. Limit defaults to 25 and hours defaults to 2.
      parameters:
        - $ref: '#/parameters/searchPeer'
        - $ref: '#/parameters/searchPrefix'
        - $ref: '#/parameters/groupBy'
        - $ref: '#/parameters/joinWhoIsPrefix'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/startTimestamp'
        - $ref: '#/parameters/endTimestamp'
      tags:
        - Updates Over Time
      responses:
        200:
          description: An array of top prefixes by number of updates in past 'hours'. Limit defaults to 25 and hours defaults to 2.
          schema:
            type: array
            items:
              $ref: '#/definitions/TopPrefixes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /updates/peer/{peerHashId}/top:
    get:
      summary: For given peer, return the top prefixes by number of updates in past 'hours'. Limit defaults to 25 and hours defaults to 2.
      description: |
        This Updates Endpoint for a given peer, return the top prefixes by number of updates in past 'hours'. Limit defaults to 25 and hours defaults to 2.
      parameters:
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/hours'
        - $ref: '#/parameters/timestamp'
      tags:
        - Updates Over Time
      responses:
        200:
          description: For given peer, return the top prefixes by number of updates in past 'hours'. Limit defaults to 25 and hours defaults to 2.
          schema:
            type: array
            items:
              $ref: '#/definitions/TopPrefixesByPeer'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /updates/top/interval/{minutes}:
    get:
      summary: Return the count of updates per interval for up to max limit size. Limit defaults to 25.
      description: |
        This Updates Endpoint returns the count of updates per interval for up to max limit size. Limit defaults to 25.
      parameters:
        - $ref: '#/parameters/minutes'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/timestamp'
      tags:
        - Updates Over Time
      responses:
        200:
          description: Return the count of updates per interval for up to max limit size. Limit defaults to 25.
          schema:
            type: array
            items:
              $ref: '#/definitions/IntervalCount'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /updates/peer/{peerHashId}/top/interval/{minutes}:
    get:
      summary: For given peer, return the count of updates per interval for up to max limit size. Limit defaults to 25.
      description: |
        This Updates Endpoint for a given peer, return the count of updates per interval for up to max limit size. Limit defaults to 25.
      parameters:
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/minutes'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/timestamp'
      tags:
        - Updates Over Time
      responses:
        200:
          description: For given peer, return the count of updates per interval for up to max limit size. Limit defaults to 25.
          schema:
            type: array
            items:
              $ref: '#/definitions/IntervalCount'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /updates/trend/interval/{seconds}:
    get:
      summary: Placeholder.
      description: |
        This Updates Endpoint placeholder.
      parameters:
        - $ref: '#/parameters/searchPeer'
        - $ref: '#/parameters/searchPrefix'
        - $ref: '#/parameters/seconds'
        - $ref: '#/parameters/startTimestamp'
        - $ref: '#/parameters/endTimestamp'
      tags:
        - Updates Over Time
      responses:
        200:
          description: Placeholder.
          schema:
            type: array
            items:
              $ref: '#/definitions/IntervalCount'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /withdrawns/top:
    get:
      summary: Get the list of top prefixes by number of prefixes withdrawn in past 'hours'. Limit defaults to 25 and hours defaults to 2.
      description: This Withdrawns Endpoint returns the top prefixes by number of prefixes withdrawn in past 'hours'. Limit defaults to 25 and hours defaults to 2.
      parameters:
        - $ref: '#/parameters/searchPeer'
        - $ref: '#/parameters/searchPrefix'
        - $ref: '#/parameters/groupBy'
        - $ref: '#/parameters/joinWhoIsPrefix'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/startTimestamp'
        - $ref: '#/parameters/endTimestamp'
      tags:
        - Withdrawns Over Time
      responses:
        200:
          description: An array of top prefixes by number of prefixes withdrawn in past 'hours'. Limit defaults to 25 and hours defaults to 2.
          schema:
            type: array
            items:
              $ref: '#/definitions/TopPrefixes'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /withdrawns/peer/{peerHashId}/top:
    get:
      summary: For given peer, return the top prefixes by number of prefixes withdrawn in past 'hours'. Limit defaults to 25 and hours defaults to 2.
      description: |
        This Withdrawns Endpoint for a given peer, return the top prefixes by number of prefixes withdrawn in past 'hours'. Limit defaults to 25 and hours defaults to 2.
      parameters:
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/hours'
        - $ref: '#/parameters/timestamp'
      tags:
        - Withdrawns Over Time
      responses:
        200:
          description: For given peer, return the top prefixes by number of prefixes withdrawn in past 'hours'. Limit defaults to 25 and hours defaults to 2.
          schema:
            type: array
            items:
              $ref: '#/definitions/TopPrefixesByPeer'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /withdrawns/top/interval/{minutes}:
    get:
      summary: Return the count of updates per interval for up to max limit size. Limit defaults to 25.
      description: |
        This Withdrawns Endpoint returns the count of updates per interval for up to max limit size. Limit defaults to 25.
      parameters:
        - $ref: '#/parameters/minutes'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/timestamp'
      tags:
        - Withdrawns Over Time
      responses:
        200:
          description: Return the count of updates per interval for up to max limit size. Limit defaults to 25.
          schema:
            type: array
            items:
              $ref: '#/definitions/IntervalCount'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /withdrawns/peer/{peerHashId}/top/interval/{minutes}:
    get:
      summary: For given peer, return the count of updates per interval for up to max limit size. Limit defaults to 25.
      description: |
        This Withdrawns Endpoint for a given peer, return the count of updates per interval for up to max limit size. Limit defaults to 25.
      parameters:
        - $ref: '#/parameters/peerHashId'
        - $ref: '#/parameters/minutes'
        - $ref: '#/parameters/limit'
        - $ref: '#/parameters/timestamp'
      tags:
        - Withdrawns Over Time
      responses:
        200:
          description: For given peer, return the count of updates per interval for up to max limit size. Limit defaults to 25.
          schema:
            type: array
            items:
              $ref: '#/definitions/IntervalCount'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
  /withdrawns/trend/interval/{seconds}:
    get:
      summary: Placeholder.
      description: |
        This Withdrawns Endpoint placeholder.
      parameters:
        - $ref: '#/parameters/searchPeer'
        - $ref: '#/parameters/searchPrefix'
        - $ref: '#/parameters/seconds'
        - $ref: '#/parameters/startTimestamp'
        - $ref: '#/parameters/endTimestamp'
      tags:
        - Withdrawns Over Time
      responses:
        200:
          description: Placeholder.
          schema:
            type: array
            items:
              $ref: '#/definitions/IntervalCount'
        400:
          description: Bad Request - Missing Required Path Parameters
          schema:
            $ref: '#/definitions/Error'
        401:
          description: Unauthorized - No Authentication
          schema:
            $ref: '#/definitions/Error'
        403:
          description: Forbidden - Authentication Correct, But You do not have persmission to perform this operation on this resource
          schema:
            $ref: '#/definitions/Error'
        404:
          description: Resource Not Found
          schema:
            $ref: '#/definitions/Error'
        405:
          description: Method not allowed/invalid on this resource
          schema:
            $ref: '#/definitions/Error'
        500:
          description: OpenBMP Server Error
          schema:
            $ref: '#/definitions/Error'
        503:
          description: OpenBMP REST API Service temporarily unavailable due to maintenance
          schema:
            $ref: '#/definitions/Error'
        504:
          description: Gateway Timeout
          schema:
            $ref: '#/definitions/Error'
        default:
          description: Unexpected error
          schema:
            $ref: '#/definitions/Error'
definitions:
  AsSubInfo:
    title: As Sub Info
    type: object
    properties:
      asn:
        type: integer
        format: int32
        description: asn value
      as_name:
        type: string
        description: as_name value
      country:
        type: string
        description: country value
      org_name:
        type: string
        description: org_name value
  TransitOriginFields:
    title: Transit Origin Fields
    type: object
    properties:
      isTransit:
        type: string
        format: byte
        description: isTransit value
      isOrigin:
        type: string
        format: byte
        description: isOrigin value
      transit_v4_prefixes:
        type: integer
        format: int64
        description: transit_v4_prefixes value
      transit_v6_prefixes:
        type: integer
        format: int64
        description: transit_v6_prefixes value
      origin_v4_prefixes:
        type: integer
        format: int64
        description: origin_v4_prefixes value
      origin_v6_prefixes:
        type: integer
        format: int64
        description: origin_v6_prefixes value 
  AsInfo:
    title: As Info
    type: object
    properties:
      as_sub_info:
        type: object
        description: As Sub Info
        items:
          $ref: '#/definitions/AsSubInfo'
      city:
        type: string
        description: city value
      state_prov:
        type: string
        description: state_prov value
  AsInfoLatLong:
    title: As Info Lat and Long
    type: object
    properties:
      as_info:
        type: object
        description: As Info
        items:
          $ref: '#/definitions/AsInfo'
      city_lat:
        type: number
        format: float
        description: city latitude value
      city_long:
        type: number
        format: float
        description: city longitude value
  UpDown:
    title: Upstreams and Downstreams
    type: object
    properties:
      upstreams:
        type: string
        description: List of Upstreams
      downstreams:
        type: string
        description: List of downstreams
  AsStatistics:
    title: As Statistics 
    type: object
    properties:
      as_sub_info:
        type: object
        description: As Sub Info
        items:
          $ref: '#/definitions/AsSubInfo'
      transit_origin_details:
        type: object
        description: As Sub Info
        items:
          $ref: '#/definitions/TransitOriginFields'
      repeats:
        type: integer
        format: int64
        description: repeats value 
      timestamp:
        type: object
        description: timestamp
        items:
          $ref: '#/definitions/Timestamp'
  AsStatisticsV4:
    title: As Statistics Generic
    type: object
    properties:
      as_sub_info:
        type: object
        description: As Sub Info
        items:
          $ref: '#/definitions/AsSubInfo'
      transit_v4_prefixes:
        type: integer
        format: int64
        description: transit_v4_prefixes value
      origin_v4_prefixes:
        type: integer
        format: int64
        description: origin_v4_prefixes value
      timestamp:
        type: object
        description: LastModified
        items:
          $ref: '#/definitions/Timestamp'
  AsStatisticsV6:
    title: As Statistics Generic
    type: object
    properties:
      as_sub_info:
        type: object
        description: As Sub Info
        items:
          $ref: '#/definitions/AsSubInfo'
      transit_v6_prefixes:
        type: integer
        format: int64
        description: transit_v6_prefixes value
      origin_v6_prefixes:
        type: integer
        format: int64
        description: origin_v6_prefixes value 
      timestamp:
        type: object
        description: LastModified
        items:
          $ref: '#/definitions/Timestamp'
  VRoutes:
    title: VRoutes
    type: object
    properties:
      RouterName:
        type: string
        description: Router Name
      PeerName:
        type: string
        description: Peer Name
      Prefix:
        type: string
        description: Prefix
      PrefixLen:
        type: integer
        format: int64
        description: Prefix Length
      Origin:
        type: string
        description: Origin
      Origin_AS:
        type: integer
        format: int64
        description: Origin_AS
      MED:
        type: integer
        format: int64
        description: MED 
      LocalPref:
        type: integer
        format: int64
        description: LocalPref 
      NH:
        type: string
        description: Next Hop
      AS_Path:
        type: string
        description: AS_Path
      ASPath_Count:
        type: integer
        format: int64
        description: AS_Path Count
      Communities:
        type: string
        description: Communities
      ExtCommunities:
        type: string
        description: Ext Communities
      ClusterList:
        type: string
        description: Cluster List
      Aggregator:
        type: string
        description: Aggregator
      PeerAddress:
        type: string
        description: Peer Address
      PeerASN:
        type: integer
        format: int64
        description: Peer ASN
      isIPv4:
        type: string
        format: byte
        description: Boolean Flag
      isPeerIPv4:
        type: string
        format: byte
        description: Boolean Flag
      isPeerVPN:
        type: string
        format: byte
        description: Boolean Flag
      LastModified:
        type: string
        description: Last Modified
      DBLastModified:
        type: string
        description: DB Last Modified
      prefix_bin:
        type: string
        format: byte
        description: Prefix Bin
      rib_hash_id:
        type: string
        description: Rib Hash Id
      path_hash_id:
        type: string
        description: Path Hash Id
      peer_hash_id:
        type: string
        description: Peer Hash Id
      router_hash_id:
        type: string
        description: Router Hash Id
      isWithdrawn:
        type: boolean
        description: Boolean Flag
      prefix_bits:
        type: string
        description: Prefix Bits
  VLSNodes:
    title: Link state Nodes
    type: object
    properties:
      ls_peers:
        type: object
        description: LS Peers Info
        items:
          $ref: '#/definitions/VLSPeers'
      IGP_RouterId:
        type: string
        description: IGP Router Id
      NodeName:
        type: string
        description: Node Name
      RouterId:
        type: string
        description: Router Id
      id:
        type: integer
        format: int64
        description: Id
      bgpls_id:
        type: integer
        format: int64
        description: BGP LS Id
      OspfAreaId:
        type: string
        description: Ospf Area Id
      ISISAreaId:
        type: string
        description: ISIS Area Id
      flags:
        type: string
        description: Boolean Flag
      timestamp:
        type: object
        description: timestamp
        items:
          $ref: '#/definitions/Timestamp'
      asn:
        type: integer
        format: int64
        description: asn
      AS_Path:
        type: string
        description: AS_Path
      LocalPref:
        type: integer
        format: int64
        description: LocalPref 
      MED:
        type: integer
        format: int64
        description: MED 
      NH:
        type: string
        description: NH
      hash_id:
        type: string
        description: Hash Id
      path_attr_hash_id:
        type: string
        description: Path Attr Hash Id
  VLSLinks:
    title: Link state Links
    type: object
    properties:
      Local_Router_Name:
        type: string
        description: Local Router Name
      Remote_Router_Name:
        type: string
        description: Remote Router Name
      Local_IGP_RouterId:
        type: string
        description: Local IGP Router Id
      Local_RouterId:
        type: string
        description: Local Router Id
      Remote_IGP_RouterId:
        type: string
        description: Remote IGP Router Id
      Remote_RouterId:
        type: string
        description: Remote Router Id
      bgpls_id:
        type: integer
        format: int64
        description: BGP LS Id
      AreaId:
        type: string
        description: Area Id
      MT_ID:
        type: integer
        format: int64
        description: MT Id
      InterfaceIP:
        type: string
        description: Interface IP
      NeighborIP:
        type: string
        description: Neighbor IP
      isIPv4:
        type: string
        format: byte
        description: isIPv4 value
      protocol:
        type: string
        enum: ["IS-IS_L1","IS-IS_L2","OSPFv2","Direct","Static","OSPFv3"]
        description: protocol
      igp_metric:
        type: integer
        format: int64
        description: IGP Metric
      local_link_id:
        type: integer
        format: int64
        description: Local Link Id
      remote_link_id:
        type: integer
        format: int64
        description: Remote Link Id
      admin_group:
        type: integer
        format: int32
        description: Admin Group
      max_link_bw:
        type: number
        format: double
        description: Max Link BW
      max_resv_bw:
        type: number
        format: double
        description: Max Resv BW
      unreserved_bw:
        type: string
        description: Unreserved BW
      te_def_metric:
        type: integer
        format: int64
        description: Id
      mpls_proto_mask:
        type: string
        enum: ["LDP","RSVP-TE"]
        description: mpls proto mask
      srig:
        type: string
        description: srig
      name:
        type: string
        description: Name
      timestamp:
        type: object
        description: timestamp
        items:
          $ref: '#/definitions/Timestamp'
      local_node_hash_id:
        type: string
        description: Local Node Hash Id
      remote_node_hash_id:
        type: string
        description: Remote Node Hash Id
      localn_igp_router_id_bin:
        type: string
        description: Localn IGP Router Id Bin
      remoten_igp_router_id_bin:
        type: string
        description: Remoten IGP Router Id Bin
      path_attr_hash_id:
        type: string
        description: Path Attr Hash Id
      peer_hash_id:
        type: string
        description: Peer Hash Id
  VLSPrefixes:
    title: Link state Prefixes
    type: object
    properties:
      Local_Router_Name:
        type: string
        description: Local Router Name
      Local_IGP_RouterId:
        type: string
        description: Local IGP Router Id
      Local_RouterId:
        type: string
        description: Local Router Id
      id:
        type: integer
        format: int64
        description: Id
      mt_id:
        type: integer
        format: int64
        description: MT Id
      Prefix:
        type: string
        description: Prefix
      prefix_len:
        type: integer
        format: int64
        description: MT Id
      ospf_route_type:
        type: string
        enum: ["Intra","Inter","Ext-1","Ext-2","NSSA-1","NSSA-2"]
        description: OSPF Route Type
      metric:
        type: integer
        format: int64
        description: Metric
      protocol:
        type: string
        enum: ["IS-IS_L1","IS-IS_L2","OSPFv2","Direct","Static","OSPFv3"]
        description: protocol
      timestamp:
        type: object
        description: timestamp
        items:
          $ref: '#/definitions/Timestamp'
      prefix_bcast_bin:
        type: string
        format: byte
        description: Prefix BCast Bin
      prefix_bin:
        type: string
        format: byte
        description: Prefix Bin
      peer_hash_id:
        type: string
        description: Peer Hash Id
  VLSPeers:
    title: Link state Peers
    type: object
    required:
      - RouterName
      - RouterIP
      - PeerName
      - PeerIP
      - protocol
      - peer_hash_id
      - router_hash_id
      - mt_id
    properties:
      RouterName:
        type: string
        description: Router Name
      RouterIP:
        type: string
        description: Router IP
      PeerName:
        type: string
        description: Peer Name
      PeerIP:
        type: string
        description: Peer IP
      protocol:
        type: string
        enum: ["IS-IS_L1","IS-IS_L2","OSPFv2","Direct","Static","OSPFv3"]
        description: protocol
      peer_hash_id:
        type: string
        description: Peer Hash Id
      router_hash_id:
        type: string
        description: Router Hash Id
      mt_id:
        type: integer
        format: int64
        description: MT Id
      geo_ip:
        type: object
        description: Geo IP Attributes
        items:
          $ref: '#/definitions/GeoIPEmbed'
  GenericCount:
    title: Generic Count
    type: object
    properties:
      PrefixCount:
        type: integer
        format: int64
        description: Generic Count that could be used as Prefix Count or Prefix Learned etc.
  PrefixDetails:
    title: Prefix Details
    type: object
    properties:
      prefix:
        type: string
        description: Prefix 
      prefix_len:
        type: integer
        format: int64
        description: Prefix Length
      prefix_bits:
        type: string
        description: Prefix Bits
  TopPrefixesByPeer:
    title: Top Prefixes By Peer
    type: object
    properties:
      Prefix:
        type: string
        description: Prefix 
      PrefixLen:
        type: string
        format: byte
        description: Prefix Length
      PeerName:
        type: string
        description: Peer Name
      PeerAddr:
        type: string
        description: Peer Address
      Count:
        type: object
        description : Count
        items:
          $ref: '#/definitions/GenericCount'
      peer_hash_id:
        type: string
        description: Peer Hash Id
      RouterName:
        type: string
        description: Router Name
  TopPrefixes:
    title: Top Prefixes
    type: object
    properties:
      top_prefixes_by_peer:
        type: object
        description: Top Prefixes by Peer Object
        items:
          $ref: '#/definitions/TopPrefixesByPeer'
      RouterAddr:
        type: string
        description: Router Address
      CollectorName:
        type: string
        description: Collector Name
      CollectorAddr:
        type: string
        description: Collector Address
      CollectorAdminID:
        type: string
        description: Collector Admin Id
      PrefixDescr:
        type: object
        description : Prefix Description Blob
        items:
          $ref: '#/definitions/BlobObject'
      OriginAS:
        type: integer
        format: int32
        description: Origin AS
  IntervalCount:
    title: Interval and Count
    type: object
    properties:
      IntervalTime:
        type: string
        description: Interval Time
      Count:
        type: object
        description : Count
        items:
          $ref: '#/definitions/GenericCount'
  IpTypeCount:
    title: IP-Type and Count
    type: object
    properties:
      IP-Type:
        type: string
        description: IP-Type
      Count:
        type: object
        description : Count
        items:
          $ref: '#/definitions/GenericCount'
  StatusTypeCount:
    title: Status Type and Count
    type: object
    properties:
      IP-Type:
        type: string
        description: Status Type
      Count:
        type: object
        description : Count
        items:
          $ref: '#/definitions/GenericCount'
  RoutersWithPeerCount:
    title: Routers with Peer Count
    type: object
    properties:
      RouterName:
        type: string
        description: Router Name
      RouterIP:
        type: string
        description: Router IP
      IPTypeCount:
        type: object
        description : IP-Type and Count
        items:
          $ref: '#/definitions/IpTypeCount'
  Timestamp:
    title: Timestamp
    type: object
    properties:
      timestamp:
        type: string
        description: Timestamp
  BlobObject:
    title: Any BLOB Object
    type: object
    properties:
      prefix_descr:
        type: object
        description: Any BLOB Object
  AsInfoAndCount:
    title: As Info and Prefixes Learned
    type: object
    properties:
      as_info:
        type: object
        description : As Info Details
        items:
          $ref: '#/definitions/AsInfo'
      Prefixes_Learned:
        type: object
        description : Prefixes Learned
        items:
          $ref: '#/definitions/GenericCount'
  AsInfoAndCountAndPeer:
    title: As Info and Prefixes Learned and Peer Details
    type: object
    properties:
      as_info_and_prefixes_learned:
        type: object
        description : As Info and prefixes learned
        items:
          $ref: '#/definitions/AsInfoAndCount'
      PeerAddr:
        type: string
        description: peer addr value
      peer_hash_id:
        type: string
        description: peer_hash_id value
  AsInfoAndPeerAsn:
    title: As Info and Peer Asn
    type: object
    properties:
      as_info:
        type: object
        description : As Info Details
        items:
          $ref: '#/definitions/AsInfo'
      PeerASN:
        type: string
        description: Peer ASN Value
  VPeers:
    title: Bgp Peers
    type: object
    properties:
      RouterName:
        type: string
        description: Router Name
      RouterIP:
        type: string
        description: Router IP
      LocalIP:
        type: string
        description: Local IP
      LocalPort:
        type: integer
        format: int64
        description: Local Port
      LocalASN:
        type: integer
        format: int64
        description: Local ASN
      LocalBGPId:
        type: string
        description: Local BGP Id
      PeerName:
        type: string
        description : Peer Name
      PeerIP:
        type: string
        description: Peer IP
      PeerPort:
        type: integer
        format: int64
        description: Peer Port
      PeerASN:
        type: integer
        format: int64
        description: Peer ASN
      PeerBGPId:
        type: string
        description: Peer BGP Id
      LocalHoldTime:
        type: integer
        format: int64
        description: Local Hold Time
      PeerHoldTime:
        type: integer
        format: int64
        description: Peer Hold Time
      isUp:
        type: string
        format: byte
        description: isUp flag
      isBMPConnected:
        type: string
        format: byte
        description: isBMPConnected flag
      isPeerIPv4:
        type: string
        format: byte
        description: isPeerIPv4 flag
      isPeerVPN:
        type: string
        format: byte
        description: isPeerVPN flag
      isPrePolicy:
        type: string
        format: byte
        description: isPrePolicy Value
      LastModified:
        type: object
        description: LastModified
        items:
          $ref: '#/definitions/Timestamp'
      LastBMPReasonCode:
        type: string
        format: byte
        description: Last BMP Reason Code
      LastDownCode:
        type: integer
        format: int64
        description: Last Down Code
      LastdownSubCode:
        type: integer
        format: int64
        description: Last down Sub Code
      LastDownMessage:
        type: string
        description: Last Down Message
      LastDownTimestamp:
        type: object
        description: LastDownTimestamp
        items:
          $ref: '#/definitions/Timestamp'
      SentCapabilities:
        type: string
        description: Sent Capabilities
      RecvCapabilities:
        type: string
        description: Recv Capabilities
      as_name:
        type: string
        description: as_name
      peer_hash_id:
        type: string
        description: Peer Hash Id
      router_hash_id:
        type: string
        description: Router Hash Id
      geo_ip_start:
        type: string
        format: byte
        description: Geo IP Start
      geo_ip:
        type: object
        description: Geo IP Attributes
        items:
          $ref: '#/definitions/GeoIPEmbed'
  VPeerPrefixReport:
    title: Bgp Peers Prefix Report
    type: object
    properties:
      RouterName:
        type: string
        description: Router Name
      PeerName:
        type: string
        description : Peer Name
      TS:
        type: object
        description: Timestamp
        items:
          $ref: '#/definitions/Timestamp'
      Rejected:
        type: integer
        format: int64
        description: Rejected
      ConfedLoop:
        type: integer
        format: int64
        description: Confed Loop
      ASLoop:
        type: integer
        format: int64
        description: AS Loop
      InvalidClusterList:
        type: integer
        format: int64
        description: Invalid Cluster List
      InvalidOriginator:
        type: integer
        format: int64
        description: Invalid Originator
      KnownPrefix_DUP:
        type: integer
        format: int64
        description: KnownPrefix_DUP
      KnownWithdraw_DUP:
        type: integer
        format: int64
        description: KnownWithdraw_DUP
      Pre_RIB:
        type: integer
        format: int64
        description: Pre RIB
      Post_RIB:
        type: integer
        format: int64
        description: Post RIB
      router_hash_id:
        type: string
        description: Router Hash Id
      peer_hash_id:
        type: string
        description: Peer Hash Id
  Router:
    title: Router
    type: object
    properties:
      name:
        type: string
        description: Router Name
      ip_address:
        type: string
        description: RouterIP
      router_AS:
        type: integer
        format: int64
        description : RouterAS
      description:
        type: string
        description: Description
      isConnected:
        type: string
        format: byte
        description: isConnected
      isPassive:
        type: string
        format: byte
        description: isPassive
      term_reason_code:
        type: integer
        format: int32
        description: LastTermCode
      term_reason_text:
        type: string
        description: LastTermReason
      init_data:
        type: string
        description: InitData
      timestamp:
        type: object
        description: LastModified
        items:
          $ref: '#/definitions/Timestamp'
      geo_ip:
        type: object
        description: Geo IP Attributes
        items:
          $ref: '#/definitions/GeoIPEmbed'
  RouterStatus:
    title: Router Status
    type: object
    properties:
      StatusType:
        type: string
        enum: ["UP","DOWN"]
        description: hash_id value
      Count:
        type: integer
        format: int32
        description: router_hash_id value
  IgpOspfSpf:
    title: Link state SPF
    type: object
    properties:
      prefix:
        type: string
        description: Router Name
      prefix_len:
        type: integer
        format: int64
        description: Prefix Length
      Type:
        type: string
        format: byte
        description: Ospf Route Type
      metric:
        type: integer
        format: int32
        description: Peer IP
      src_router_id:
        type: string
        description: Src Router Id
      nei_router_id:
        type: string
        description: Nei Router Id
      path_router_ids:
        type: string
        description: Path Router Ids
      path_hash_ids:
        type: string
        description: Path Hash Ids
      neighbor_addr:
        type: string
        description: Neighbor Address
      peer_hash_id:
        type: string
        description: Peer Hash Id
  IgpIsisSpf:
    title: Link state SPF
    type: object
    properties:
      igp_ospf_spf:
        type: object
        description: Igp Ospf Spf
        items:
          $ref: '#/definitions/IgpOspfSpf'
      mt_id:
        type: integer
        format: int64
        description: Mt Id
  GeoIPEmbed:
    title: GeoIPEmbed
    type: object
    properties:
      ipStart:
        type: string
        description: IP address start value
      ipEnd:
        type: string
        description: IP address end value
      addrType:
        type: string
        description: Type of Address
        enum: ["ipv4","ipv6"]
      country:
        type: string
        description: Country Code 
      stateProv:
        type: string
        description: Name of the State Province
      city:
        type: string
        description: Name of the City
      latitude:
        type: number
        format: float
        description : Latitude Coordinate
      longitude:
        type: number
        format: float
        description: Longitude Coordinate
      timezoneOffset:
        type: number
        format: float
        description: Timezone Offset
      timezoneName:
        type: string
        description: Name of the Timezone
      ispName:
        type: string
        description: Name of the ISP
      connectionType:
        type: string
        description: Type of Connection
        enum: ["dialup","isdn","cable","dsl","fttx","wireless"]
      organizationName:
        type: string
        description: Name of the Organizati
      ipStartBin:
        type: string
        description: ip address start value in binary
      ipEndBin:
        type: string
        description: ip address end value in binary 
  GenWhoIsAsn:
    title: Gen Who Is Asn 
    type: object
    properties:
      as_info:
        type: object
        description: As Info
        items:
          $ref: '#/definitions/AsInfo'
      timestamp:
        type: object
        description: timestamp
        items:
          $ref: '#/definitions/Timestamp'
      org_id:
        type: string
        description: Organization Id
      remarks:
        type: string
        description: Remarks
      address:
        type: string
        description: Address
      postal_code:
        type: string
        description: Postal Code
      raw_output:
        type: string
        description: Raw Output
      source:
        type: string
        description: Source name
      transit_origin_fields:
        type: object
        description: Transit Origin Fields
        items:
          $ref: '#/definitions/TransitOriginFields'
  GenWhoIsRoute:
    title: Gen Who Is Route 
    type: object
    properties:
      prefix:
        type: string
        description: Prefix
      prefix_len:
        type: integer
        format: int64
        description: Prefix Length
      descr:
        type: object
        description : Description Blob
        items:
          $ref: '#/definitions/BlobObject'
      origin_as:
        type: integer
        format: int32
        description: Origin AS
      source:
        type: string
        description: Source name
  GenAsnStatsEmbed:
    title: Gen Asn Stats Embed
    type: object
    properties:
      asn:
        type: integer
        format: int64
        description: Access Service Network
      isTransit:
        type: integer
        format: int32
        description: Boolean Flag for Transit
      isOrigin:
        type: integer
        format: int32
        description: Boolean Flag for Origin
      transitV4Prefixes:
        type: integer
        format: int64
        description: Transit Prefixes of type V4 
      transitV6Prefixes:
        type: integer
        format: int64
        description: Transit Prefixes of type V6
      originV4Prefixes:
        type: integer
        format: int64
        description: Origin Prefixes of type V4
      originV6Prefixes:
        type: integer
        format: int64
        description : Origin Prefixes of type V6
      repeats:
        type: integer
        format: int64
        description: Repeats Value
      timestamp:
        type: string
        description: Timestamp String
  Community:
    type: object
    properties:
      message:
        type: string
      attributes:
        type: object
        additionalProperties: {}
  DNS:
    type: object
    properties:
      message:
        type: string
      attributes:
        type: object
        additionalProperties: {}
  Error:
    type: object
    properties:
      code:
        type: string
        description: Application specific error codes
        #
        # When adding a new error, add a new enum below as well as make corresponding entry in ErrorDetails
        #
        enum:
          - DATASOURCE_CONNECT_FAILURE
          - INVALID_PREFIX_BITS
          - VALIDATION_FAILED_TYPE1
          - VALIDATION_FAILED_TYPE2
          - REVERSE_PROXY_ERROR
          - LOAD_BALANCER_ERROR
          - NOT_READY_YET
          - USAGE_LIMIT_REACHED
          - FETCH_LIMIT_EXCEEDED
          - INVALID_COMMUNITY_VALUE
          - INVALID_P1
          - INVALID_HOSTNAME
          - INVALID_ASN
          - MISSING_REQD_PARAMETER_ASN
          - INVALID_PEERHASHID
          - MISSING_REQD_PARAMETER_PEERHASHID
          - INVALID_IPADDRESS
          - MISSING_REQD_PARAMETER_IP
          - MISSING_REQD_PARAMETER_LIMIT
          - MISSING_REQD_PARAMMETER_PAGE
          - INCORRECT_WHERE_CLAUSE
          - INCORRECT_SORT_VALUE
          - INCORRECT_SORT_DIRECTION_VALUE
          - INCORRECT_ORDER_BY_CLAUSE
          - INCORRECT_GROUP_BY_CLAUSE
          - INVALID_IP_START
          - INVALID_IP_END
          - COUNTRY_INVALID
          - STATE_PROV_INVALID
          - CITY_INVALID
          - INCORRECT_LAT_VALUE
          - INCORRECT_LONG_VALUE
          - INVALID_TIMEZONE
          - INVALID_TIMEZONE_OFFSET
          - INVALID_ISP
          - UNAVAILABLE_COLUMN
          - VALUE_SIZE_EXCEED_FOR_COLUMN
          - MISSING_REQD_PARAMETER_IP_START
          - MISSING_REQD_PARAMETER_COLUMN
          - MISSING_REQD_PARAMETER_VALUE
          - MISSING_REQD_PARAMETER_COUNTRY
          - MISSING_REQD_PARAMETER_CITY
          - INCORRECT_BOOLEAN_VALUE
          - MISSING_REQD_PARAMETER_MTID
          - INVALID_MTID
          - MISSING_REQD_PARAMETER_ROUTERID
          - INVALID_ROUTERID
          - MISSING_REQD_PARAMETER_LOCALIP
          - MISSING_REQD_PARAMETER_PEERIP
          - MISSING_REQD_PARAMETER_ROUTERIP
          - MISSING_REQD_PARAMETER_ROUTERHASHID
          - INVALID_ROUTERHASHID
          - MISSING_REQD_PARAMETER_PREFIX
          - MISSING_REQD_PARAMETER_LENGTH
          - INVALID_LENGTH
          - INVALID_HOURS
          - INVALID_TIMESTAMP
          - INVALID_PAGE
          - INVALID_SEARCHPEER
          - INVALID_SEARCHPREFIX
          - MISSING_REQD_PARAMETER_MINUTES
          - INVALID_MINUTES
          - MISSING_REQD_PARAMRTER_SECONDS
          - INVALID_SECONDS
          - MISSING_REQD_PARAMETER_PREFIX_LEN
          - INVALID_PREFIX_LEN
      message:
        type: string
      description:
        type: string

  ErrorDetails:
    -
      DATASOURCE_CONNECT_FAILURE:
        code: 10001
        message: "Datasource x.x.x.x is unreachable"
        description: "Either the datasource address is incorrect / invalid database name / user name / password"
    -  
      INVALID_PREFIX_BITS:
        code: 10002
        message: "Invalid prefix bits specified, must be between 1 - 128"
        description: "The prefix bits should be a meaningful number"
    -  
      VALIDATION_FAILED_TYPE1:
        code: 10003
        message: "Invalid username"
        description: "More details"
    -
      VALIDATION_FAILED_TYPE2:
        code: 10004
        message: "Invalid password"
        description: "More details"
    -
      REVERSE_PROXY_ERROR:
        code: 10005
        message: "Reverse Proxy Error"
        description: "More details"
    -  
      LOAD_BALANCER_ERROR:
        code: 10006
        message: "Invalid password"
        description: "More details"
    -  
      NOT_READY_YET:
        code: 10007
        message: "Data not ready now, try again later"
        description: "More details"
    -  
      USAGE_LIMIT_REACHED:
        code: 10008
        message: "Free Usage Limit Exceeded, try again tomorrow"
        description: "More details"
    -  
      FETCH_LIMIT_EXCEEDED:
        code: 10009
        message: "Maximum value for limit is 2000"
        description: "More details"
    -  
      INVALID_COMMUNITY_VALUE:
        code: 10010
        message: "Unacceptable value for Community"
        description: "More details"
    -  
      INVALID_P1:
        code: 10011
        message: "Unacceptable value for P1"
        description: "More details"
    -  
      INVALID_HOSTNAME:
        code: 10012
        message: "Unacceptable value for Host Name"
        description: "More details"
    -  
      INVALID_ASN:
        code: 10013
        message: "Unacceptable value for ASN"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_ASN:
        code: 10014
        message: "Missing value for Path Parameter Asn"
        description: "More details"
    -  
      INVALID_PEERHASHID:
        code: 10015
        message: "Unacceptable value for Peer Hash Id"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_PEERHASHID:
        code: 10016
        message: "Missing value for Path Parameter PeerHashId"
        description: "More details"
    -  
      INVALID_IPADDRESS:
        code: 10017
        message: "Unacceptable value for IP Address"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_IP:
        code: 10018
        message: "Missing value for Path Parameter IP"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_LIMIT:
        code: 10019
        message: "Missing value for Path Parameter Limit"
        description: "More details"
    -  
      MISSING_REQD_PARAMMETER_PAGE:
        code: 10020
        message: "Missing value for Path Parameter Page"
        description: "More details"
    -  
      INCORRECT_WHERE_CLAUSE:
        code: 10021
        message: "Unacceptable value for Where Clause"
        description: "More details"
    -  
      INCORRECT_SORT_VALUE:
        code: 10022
        message: "Unacceptable value for Sort"
        description: "More details"
    -  
      INCORRECT_SORT_DIRECTION_VALUE:
        code: 10023
        message: "Unacceptable value for Sort Direction"
        description: "More details"
    -  
      INCORRECT_ORDER_BY_CLAUSE:
        code: 10024
        message: "Unacceptable value for Order By"
        description: "More details"
    -  
      INCORRECT_GROUP_BY_CLAUSE:
        code: 10025
        message: "Unacceptable value for Group By"
        description: "More details"
    -  
      INVALID_IP_START:
        code: 10026
        message: "Unacceptable value for IP Start"
        description: "More details"
    -  
      INVALID_IP_END:
        code: 10027
        message: "Unacceptable value for IP end"
        description: "More details"
    -  
      COUNTRY_INVALID:
        code: 10028
        message: "Unacceptable value for Country"
        description: "More details"
    -  
      STATE_PROV_INVALID:
        code: 10029
        message: "Unacceptable value for State Prov"
        description: "More details"
    -  
      CITY_INVALID:
        code: 10030
        message: "Unacceptable value for City"
        description: "More details"
    -  
      INCORRECT_LAT_VALUE:
        code: 10031
        message: "Unacceptable Latitude Value"
        description: "More details"
    -  
      INCORRECT_LONG_VALUE:
        code: 10032
        message: "Unacceptable Longitude value"
        description: "More details"
    -  
      INVALID_TIMEZONE:
        code: 10033
        message: "Unacceptable value for Timezone"
        description: "More details"
    -  
      INVALID_TIMEZONE_OFFSET:
        code: 10034
        message: "Unacceptable value for Timezone offset"
        description: "More details"
    -  
      INVALID_ISP:
        code: 10035
        message: "Unacceptable value for ISP"
        description: "More details"
    -  
      UNAVAILABLE_COLUMN:
        code: 10036
        message: "This Column is not available in the database"
        description: "More details"
    -  
      VALUE_SIZE_EXCEED_FOR_COLUMN:
        code: 10037
        message: "Unacceptable value for Column Value Size"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_IP_START:
        code: 10038
        message: "Missing value for Path Parameter IP Start"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_COLUMN:
        code: 10039
        message: "Missing value for Path Parameter Column"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_VALUE:
        code: 10040
        message: "Missing value for Path Parameter Value"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_COUNTRY:
        code: 10041
        message: "Missing value for Path Parameter Country"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_CITY:
        code: 10042
        message: "Missing value for Path Parameter City"
        description: "More details"
    -  
      INCORRECT_BOOLEAN_VALUE:
        code: 10043
        message: "Unacceptable value for Boolean Variable"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_MTID:
        code: 10044
        message: "Unacceptable value for MT Id"
        description: "More details"
    -  
      INVALID_MTID:
        code: 10045
        message: "Unacceptable value for MT Id"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_ROUTERID:
        code: 10046
        message: "Missing value for Path Parameter RouterId"
        description: "More details"
    -  
      INVALID_ROUTERID:
        code: 10047
        message: "Unacceptable value for RouterId"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_LOCALIP:
        code: 10048
        message: "Missing value for Path Parameter LocalIP"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_PEERIP:
        code: 10049
        message: "Missing value for Path Parameter PeerIP"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_ROUTERIP:
        code: 10050
        message: "Missing value for Path Parameter RouterIP"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_ROUTERHASHID:
        code: 10051
        message: "Missing value for Path Parameter RouterHashId"
        description: "More details"
    -  
      INVALID_ROUTERHASHID:
        code: 10052
        message: "Unacceptable value for RouterHashId"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_PREFIX:
        code: 10053
        message: "Missing value for Path Parameter Prefix"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_LENGTH:
        code: 10054
        message: "Missing value for Path Parameter Length"
        description: "More details"
    -  
      INVALID_LENGTH:
        code: 10055
        message: "Unacceptable value for Length"
        description: "More details"
    -  
      INVALID_HOURS:
        code: 10056
        message: "Unacceptable value for Hours"
        description: "More details"
    -  
      INVALID_TIMESTAMP:
        code: 10057
        message: "Unacceptable value for Timestamp"
        description: "More details"
    -  
      INVALID_PAGE:
        code: 10058
        message: "Unacceptable value for Page"
        description: "More details"
    -  
      INVALID_SEARCHPEER:
        code: 10059
        message: "Unacceptable value for Search Peer"
        description: "More details"
    -  
      INVALID_SEARCHPREFIX:
        code: 10060
        message: "Unacceptable value for Search Prefix"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_MINUTES:
        code: 10061
        message: "Missing value for Path Parameter Minutes"
        description: "More details"
    -  
      INVALID_MINUTES:
        code: 10062
        message: "Unacceptable value for Minutes"
        description: "More details"
    -  
      MISSING_REQD_PARAMRTER_SECONDS:
        code: 10063
        message: "Missing value for Path Parameter Seconds"
        description: "More details"
    -  
      INVALID_SECONDS:
        code: 10064
        message: "Unacceptable value for Seconds"
        description: "More details"
    -  
      MISSING_REQD_PARAMETER_PREFIX_LEN:
        code: 10065
        message: "Missing value for Path Parameter Prefix Length"
        description: "More details"
    -  
      INVALID_PREFIX_LEN:
        code: 10066
        message: "Unacceptable value for Prefix Length"
        description: "More details"

parameters:
  limit:
    name: limit
    in: query
    description: The Limit Value in SQL query
    type: integer
    format: int32
    required: false
  topTransit:
    name: topTransit
    in: query
    description: The Limit Value for Transit and Order By Transit
    type: integer
    format: int32
    required: false
  topOrigin:
    name: topOrigin
    in: query
    description: The Limit Value for Origin and Order by Origin
    type: integer
    format: int32
    required: false
  where:
    name: where
    in: query
    description: The Where clause in SQL query
    type: string
    required: false
  orderBy:
    name: orderBy
    in: query
    description: The Order By clause in SQL query
    type: string
    required: false
  groupBy:
    name: groupBy
    in: query
    description: The Group By clause in SQL query
    type: string
    required: false
  withGeo:
    name: withGeo
    in: query
    description: Boolean Flag 
    type: boolean
    required: false
  brief:
    name: brief
    in: query
    description: Boolean Flag
    type: boolean
    required: false
  distinct:
    name: distinct
    in: query
    description: Boolean Flag
    type: boolean
    required: false
  aggregates:
    name: agg
    in: query
    description: Boolean Flag
    type: boolean
    required: false
  hours:
    name: hours
    in: query
    description: The value in hours
    type: integer
    format: int32
    required: false
  timestamp:
    name: timestamp
    in: query
    description: The timestamp string
    type: string
    required: false
  startTimestamp:
    name: startTimestamp
    in: query
    description: The start timestamp string
    type: string
    required: false
  endTimestamp:
    name: endTimestamp
    in: query
    description: The end timestamp string
    type: string
    required: false
  searchPeer:
    name: searchPeer
    in: query
    description: The searchPeer value
    type: string
    required: false
  searchPrefix:
    name: searchPrefix
    in: query
    description: The searchPrefix value
    type: string
    required: false
  joinWhoIsPrefix:
    name: joinWhoIsPrefix
    in: query
    description: Boolean Flag
    type: boolean
    required: false
  p1:
    name: p1
    in: path
    description: P1
    type: integer
    format: int32
    required: true
  asn:
    name: asn
    in: path
    description: ASN Number
    type: integer
    format: int32
    required: true
  peerHashId:
    name: peerHashId
    in: path
    description: The peerHashId value
    type: string
    required: true
  hostname:
    name: hostname
    in: path
    description: Host Name
    type: string
    required: true
  routerHashId:
    name: routerHashId
    in: path
    description: The routerHashId value
    type: string
    required: true
  ipAddress:
    name: ipAddress
    in: path
    description: This could be localIP/ remoteIP/ routerIP/ peerIP
    type: string
    required: true
  prefix:
    name: prefix
    in: path
    description: Prefix Value
    type: string
    required: true
  length:
    name: length
    in: path
    description: Length Value
    type: integer
    format: int32
    required: true
  routerId:
    name: routerId
    in: path
    description: The routerId value
    type: string
    required: true
  mtId:
    name: mtId
    in: path
    description: The mtId value
    type: integer
    format: int64
    required: true
  minutes:
    name: minutes
    in: path
    description: The value in minutes
    type: integer
    format: int32
    required: true
  seconds:
    name: seconds
    in: path
    description: The value in seconds
    type: number
    format: float
    required: true



